{
  "name": "@cleomacs/dbal",
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "build": "yarn run clean && yarn run compile",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "jest": "^27.4.5",
    "ts-jest": "^27.1.2"
  },
  "dependencies": {
    "@cleomacs/db": "^1.0.0"
  }
}
